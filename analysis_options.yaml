include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_single_quotes: true
    avoid_print: true

analyzer:
  exclude:
    - "**/*.g.dart"
  errors:
    unused_field: ignore
    unused_local_variable: ignore
